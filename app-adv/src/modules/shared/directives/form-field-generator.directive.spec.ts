import { FormFieldGeneratorDirective } from './form-field-generator.directive';

describe('FormFieldGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
